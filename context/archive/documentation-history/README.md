# Archive

This folder contains superseded phase-baseline docs and historical matrices that are no longer the canonical source of truth.

Use the docs under the main `product/`, `architecture/`, `operations/`, and `governance/` folders first.
